<h1>All Tags</h1>

<ul id="tag">
  <% @tag.each do |tag| %>
    <li>
      <%= link_to tag.name, tag_path(tag) %>
      <%= link_to "Delete tag", tag_path(tag), method: :delete %>
    </li>
  <% end %>
</ul>
